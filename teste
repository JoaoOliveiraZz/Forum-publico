<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
</head>
<body>
        <?php

            include_once('connection.php');

            //Criando uma query de Select ordenando os usuários por ID
            $sql = "SELECT * FROM comentario ORDER BY id_coment DESC";

            $result = $con -> query($sql);

        ?>
        <table>
            <thead>
                <tr>
                    <th>Comentario</th>
                </tr>
            </thead>
            <tbody>
                <?php

                    while($DataUsers = mysqli_fetch_assoc($result)){ //O mysqli_fetch_assoc cria uma matriz associativa com o resultado retornado do banco de dados
                        echo "<tr>";
                        echo "<td>". $DataUsers['comentario']."</td>"; 
                        
                        echo "</tr>";//Aqui você deve colocar no lugar de $DataUsers['nome'] as colunas referentes as tabelas do seu bando de dados :)
                        //Caso aja outras colunas além de nome e email, apenas repita os comandos acima :)
                    }

                ?>
            </tbody>
        </table>

</body>
</html>